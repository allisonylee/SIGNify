# Pinned for the sign-language backend.
#
# mediapipe: DO NOT upgrade to 1.0.x. On macOS arm64 the 1.0.x wheels abort
# (SIGABRT) in every vision task -- their TFLite calculators request a Metal
# graph service that is never registered. 0.10.35 works on the CPU delegate.
# Evidence: backend/outputs/stage0_api_matrix.txt
mediapipe==0.10.35

# Pulled in by mediapipe, pinned to what Stage 0 was verified against.
numpy==2.5.3
opencv-contrib-python==5.0.0.93

# Backend
fastapi==0.141.1
uvicorn==0.53.0
# google-genai pins websockets<17; 16.1.1 verified working with the
# ElevenLabs streaming client.
websockets==16.1.1
python-dotenv==1.2.3

# Stage 4 -- LLM layer
# gemini-3.5-flash-lite measured at 566 ms; it REJECTS thinking_config (400).
# gemini-2.5-flash-lite is retired (404) despite still being listed.
google-genai==2.24.0
httpx==0.28.1
